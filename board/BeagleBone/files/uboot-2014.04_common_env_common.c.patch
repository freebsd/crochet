--- a/common/env_common.c	2014-04-10 09:21:17.000000000 +0800
+++ b/common/env_common.c	2014-04-10 09:21:41.000000000 +0800
@@ -59,9 +59,9 @@
 
 const uchar *env_get_addr(int index)
 {
-	if (gd->env_valid)
-		return (uchar *)(gd->env_addr + index);
-	else
+//	if (gd->env_valid)
+//		return (uchar *)(gd->env_addr + index);
+//	else
 		return &default_environment[index];
 }
 
